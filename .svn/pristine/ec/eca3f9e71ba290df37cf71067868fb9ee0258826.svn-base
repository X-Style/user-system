import * as loginMutation from '../views/login/vuex/mutations'

const globalMutation = {
  LOADING_CHANGE (state, action) {
    state['XHR_STATUS'] = action
  },
  MESSAGE_CHANGE (state, action) {
    state['MESSAGE_STATUS'] = action
  }
}
export default {
  ...loginMutation,
  ...globalMutation
}
